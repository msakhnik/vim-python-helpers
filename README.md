# Vim Helpers

 - :__pdb__ Insert `import pdb; pdb.set_trace()` breakpoint. 
 - __:rpdb__ Insert `import rpdb; rpdb.set_trace()` the remote breakepoint 
 - __:Dtos__ Replace double quotes with single quotes (except docstrings)
